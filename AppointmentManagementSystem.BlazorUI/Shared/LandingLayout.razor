@inherits LayoutComponentBase
@using Radzen
@inject NavigationManager Navigation
@inject IJSRuntime JSRuntime

<RadzenLayout Style="min-height: 100vh; position: relative;">
    <!-- Fixed Navigation Header with Smooth Scroll Links -->
    <RadzenHeader Style="position: fixed; top: 0; left: 0; right: 0; z-index: 9999; background: rgba(255,255,255,0.98); backdrop-filter: blur(15px); box-shadow: 0 2px 20px rgba(0,0,0,0.08); transition: all 0.3s; height: auto; max-width: 100vw; overflow-x: hidden;">
        <RadzenStack Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center" JustifyContent="JustifyContent.SpaceBetween" Style="padding: 12px 16px; max-width: 1400px; margin: 0 auto; width: 100%; overflow-x: hidden;" class="header-content">
            <!-- Logo -->
            <a href="/" style="text-decoration: none; display: flex; align-items: center;">
                <RadzenStack Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center" Gap="12px">
                    <div style="width: 40px; height: 40px; border-radius: 10px; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); display: flex; align-items: center; justify-content: center; box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);">
                        <RadzenIcon Icon="event_available" Style="font-size: 22px; color: white;" />
                    </div>
                    <RadzenText TextStyle="TextStyle.H6" Style="margin: 0; font-weight: 700; color: var(--rz-primary); font-size: 18px;" class="logo-text">
                        Aptiva Plan
                    </RadzenText>
                </RadzenStack>
            </a>
            
            <!-- Desktop Navigation -->
            <RadzenStack Orientation="Orientation.Horizontal" Gap="32px" AlignItems="AlignItems.Center" class="desktop-nav">
                <span @onclick="@(() => ScrollToSection("#hero"))" style="text-decoration: none; color: var(--rz-text-color); font-weight: 500; transition: color 0.3s; cursor: pointer;" class="nav-link">
                    Ana Sayfa
                </span>
                <span @onclick="@(() => ScrollToSection("#features"))" style="text-decoration: none; color: var(--rz-text-color); font-weight: 500; transition: color 0.3s; cursor: pointer;" class="nav-link">
                    Özellikler
                </span>
                <span @onclick="@(() => ScrollToSection("#how-it-works"))" style="text-decoration: none; color: var(--rz-text-color); font-weight: 500; transition: color 0.3s; cursor: pointer;" class="nav-link">
                    Nasıl Çalışır
                </span>
                <span @onclick="@(() => ScrollToSection("#categories"))" style="text-decoration: none; color: var(--rz-text-color); font-weight: 500; transition: color 0.3s; cursor: pointer;" class="nav-link">
                    Kategoriler
                </span>
            </RadzenStack>

            <!-- Auth Buttons -->
            <RadzenStack Orientation="Orientation.Horizontal" Gap="12px" AlignItems="AlignItems.Center" class="auth-buttons">
                <a href="/login" style="text-decoration: none;">
                    <RadzenButton Text="Giriş" ButtonStyle="ButtonStyle.Light" Size="ButtonSize.Medium" class="btn-compact" />
                </a>
                <a href="/register" style="text-decoration: none;">
                    <RadzenButton Text="Başla" ButtonStyle="ButtonStyle.Primary" Size="ButtonSize.Medium" class="btn-compact" />
                </a>
            </RadzenStack>

            <!-- Mobile Menu Button -->
            <RadzenButton Icon="@(mobileMenuOpen ? "close" : "menu")" 
                         ButtonStyle="ButtonStyle.Light" 
                         Size="ButtonSize.Medium"
                         Click="ToggleMobileMenu"
                         class="mobile-menu-btn" />
        </RadzenStack>

        <!-- Mobile Menu Dropdown -->
        @if (mobileMenuOpen)
        {
            <div class="mobile-menu">
                <RadzenStack Orientation="Orientation.Vertical" Gap="16px" Style="padding: 24px;">
                    <div @onclick="@(() => ScrollToSection("#hero"))" class="mobile-nav-link">
                        Ana Sayfa
                    </div>
                    <div @onclick="@(() => ScrollToSection("#features"))" class="mobile-nav-link">
                        Özellikler
                    </div>
                    <div @onclick="@(() => ScrollToSection("#how-it-works"))" class="mobile-nav-link">
                        Nasıl Çalışır
                    </div>
                    <div @onclick="@(() => ScrollToSection("#categories"))" class="mobile-nav-link">
                        Kategoriler
                    </div>
                    <RadzenDivider Style="margin: 8px 0;" />
                    <RadzenStack Orientation="Orientation.Horizontal" Gap="12px">
                        <a href="/login" style="flex: 1; text-decoration: none;">
                            <RadzenButton Text="Giriş Yap" ButtonStyle="ButtonStyle.Light" Size="ButtonSize.Medium" Style="width: 100%;" />
                        </a>
                        <a href="/register" style="flex: 1; text-decoration: none;">
                            <RadzenButton Text="Kayıt Ol" ButtonStyle="ButtonStyle.Primary" Size="ButtonSize.Medium" Style="width: 100%;" />
                        </a>
                    </RadzenStack>
                </RadzenStack>
            </div>
        }
    </RadzenHeader>

    <!-- Main Content -->
    <RadzenBody Style="padding-top: 70px; min-height: calc(100vh - 70px); position: relative; z-index: 1;">
        @Body
    </RadzenBody>

    <!-- Modern Footer -->
    <RadzenFooter class="landing-footer">
        <!-- Decorative Background -->
        <div class="footer-decoration"></div>
        
        <div class="footer-container">
            <RadzenRow Gap="32px" class="footer-main-row">
                <!-- Company Info -->
                <RadzenColumn Size="12" SizeMD="6" SizeLG="4" class="footer-col">
                    <RadzenStack Orientation="Orientation.Vertical" Gap="20px">
                        <RadzenStack Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center" Gap="12px">
                            <div class="footer-brand-icon">
                                <RadzenIcon Icon="event_available" Style="font-size: 24px;" />
                            </div>
                            <RadzenText TextStyle="TextStyle.H5" class="footer-brand-text">
                                Aptiva Plan
                            </RadzenText>
                        </RadzenStack>
                        <RadzenText class="footer-description">
                            Modern randevu yönetim sistemi ile işletmenizi dijitalleştirin. Müşterilerinize 7/24 online randevu imkanı sunun.
                        </RadzenText>
                        <RadzenStack Orientation="Orientation.Horizontal" Gap="12px" class="social-icons-container">
                            <div class="social-icon">
                                <RadzenIcon Icon="facebook" Style="font-size: 20px;" />
                            </div>
                            <div class="social-icon">
                                <RadzenIcon Icon="mail" Style="font-size: 20px;" />
                            </div>
                            <div class="social-icon">
                                <RadzenIcon Icon="phone" Style="font-size: 20px;" />
                            </div>
                        </RadzenStack>
                    </RadzenStack>
                </RadzenColumn>

                <!-- Quick Links -->
                <RadzenColumn Size="6" SizeMD="3" SizeLG="2" class="footer-col">
                    <RadzenStack Orientation="Orientation.Vertical" Gap="16px">
                        <RadzenText TextStyle="TextStyle.H6" class="footer-heading">
                            Hızlı Linkler
                        </RadzenText>
                        <span @onclick="@(() => ScrollToSection("#features"))" class="footer-link">Özellikler</span>
                        <span @onclick="@(() => ScrollToSection("#how-it-works"))" class="footer-link">Nasıl Çalışır</span>
                        <span @onclick="@(() => ScrollToSection("#categories"))" class="footer-link">Kategoriler</span>
                        <a href="/login" class="footer-link">Giriş Yap</a>
                    </RadzenStack>
                </RadzenColumn>

                <!-- Company -->
                <RadzenColumn Size="6" SizeMD="3" SizeLG="2" class="footer-col">
                    <RadzenStack Orientation="Orientation.Vertical" Gap="16px">
                        <RadzenText TextStyle="TextStyle.H6" class="footer-heading">
                            Şirket
                        </RadzenText>
                        <a href="/register" class="footer-link">İşletme Kaydı</a>
                        <a href="#" class="footer-link">Hakkımızda</a>
                        <a href="#" class="footer-link">Blog</a>
                        <a href="#" class="footer-link">Kariyer</a>
                    </RadzenStack>
                </RadzenColumn>

                <!-- Contact -->
                <RadzenColumn Size="12" SizeMD="6" SizeLG="4" class="footer-col">
                    <RadzenStack Orientation="Orientation.Vertical" Gap="20px">
                        <RadzenText TextStyle="TextStyle.H6" class="footer-heading">
                            İletişim
                        </RadzenText>
                        <RadzenStack Orientation="Orientation.Vertical" Gap="12px">
                            <RadzenStack Orientation="Orientation.Horizontal" Gap="12px" AlignItems="AlignItems.Start" class="contact-item">
                                <RadzenIcon Icon="email" Style="margin-top: 2px; opacity: 0.8; flex-shrink: 0;" />
                                <RadzenText class="contact-text">
                                    info@aptivaplan.com
                                </RadzenText>
                            </RadzenStack>
                            <RadzenStack Orientation="Orientation.Horizontal" Gap="12px" AlignItems="AlignItems.Start" class="contact-item">
                                <RadzenIcon Icon="phone" Style="margin-top: 2px; opacity: 0.8; flex-shrink: 0;" />
                                <RadzenText class="contact-text">
                                    0850 123 45 67
                                </RadzenText>
                            </RadzenStack>
                            <RadzenStack Orientation="Orientation.Horizontal" Gap="12px" AlignItems="AlignItems.Start" class="contact-item">
                                <RadzenIcon Icon="location_on" Style="margin-top: 2px; opacity: 0.8; flex-shrink: 0;" />
                                <RadzenText class="contact-text">
                                    İstanbul, Türkiye
                                </RadzenText>
                            </RadzenStack>
                        </RadzenStack>
                    </RadzenStack>
                </RadzenColumn>
            </RadzenRow>

            <RadzenDivider class="footer-divider" />

            <div class="footer-bottom">
                <RadzenText class="footer-copyright">
                    © 2025 Aptiva Plan. Tüm hakları saklıdır.
                </RadzenText>
                <div class="footer-legal-links">
                    <a href="#" class="legal-link">Gizlilik Politikası</a>
                    <a href="#" class="legal-link">Kullanım Şartları</a>
                    <a href="#" class="legal-link">Çerez Politikası</a>
                </div>
            </div>
        </div>
    </RadzenFooter>
</RadzenLayout>

<style>
    /* Base styles */
    * {
        box-sizing: border-box;
    }

    /* Prevent all horizontal overflow */
    html {
        overflow-x: hidden;
        width: 100%;
    }

    body {
        overflow-x: hidden;
        width: 100%;
        position: relative;
    }

    /* Fix for all Radzen components */
    .rz-layout,
    .rz-header,
    .rz-body,
    .rz-footer,
    .rz-stack,
    .rz-row {
        max-width: 100vw !important;
        overflow-x: hidden;
    }

    /* Mobile specific global fixes */
    @@media (max-width: 768px) {
        /* Prevent text overflow */
        h1, h2, h3, h4, h5, h6, p, span, div {
            word-wrap: break-word;
            overflow-wrap: break-word;
            hyphens: auto;
        }

        /* Ensure images don't overflow */
        img {
            max-width: 100%;
            height: auto;
        }

        /* Fix for any absolute positioned elements */
        [style*="position: absolute"] {
            max-width: 100vw;
        }
    }

    /* Header always on top */
    .rz-header {
        z-index: 9999 !important;
        position: fixed !important;
        top: 0 !important;
        left: 0 !important;
        right: 0 !important;
    }

    /* Responsive Navigation */
    .header-content {
        padding: 12px 20px !important;
        width: 100%;
        max-width: 100%;
        overflow: hidden;
        position: relative;
        z-index: 10000;
    }

    @@media (max-width: 768px) {
        /* Header fixed positioning on mobile */
        .rz-header {
            height: auto !important;
            min-height: 60px;
        }

        .header-content {
            padding: 8px 12px !important;
            gap: 8px !important;
            min-height: 60px;
        }

        /* Body padding adjustment for mobile */
        .rz-body {
            padding-top: 60px !important;
        }

        .desktop-nav {
            display: none !important;
        }
        
        .logo-text {
            display: none !important;
        }
        
        .mobile-menu-btn {
            display: flex !important;
        }
        
        .auth-buttons {
            display: none !important;
        }

        /* Header brand on mobile */
        .header-brand {
            display: flex !important;
            flex: 1;
            min-width: 0;
        }

        .brand-text {
            display: none !important;
        }

        /* Menu toggle button */
        .menu-toggle-btn {
            flex-shrink: 0;
            width: 44px;
            height: 44px;
        }
    }

    @@media (min-width: 769px) {
        .mobile-menu-btn {
            display: none !important;
        }

        .logo-text {
            display: block !important;
        }
    }

    .mobile-menu {
        background: white;
        box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        animation: slideDown 0.3s ease;
        width: 100%;
        max-width: 100vw;
        overflow-x: hidden;
        position: relative;
        z-index: 9998;
    }

    .mobile-menu .rz-stack {
        width: 100%;
        max-width: 100%;
    }

    .mobile-nav-link {
        color: var(--rz-text-color);
        text-decoration: none;
        font-weight: 500;
        padding: 12px 16px;
        border-radius: 8px;
        transition: all 0.3s;
        display: block;
        cursor: pointer;
        position: relative;
        overflow: hidden;
    }

    .mobile-nav-link::before {
        content: '';
        position: absolute;
        left: 0;
        top: 0;
        height: 100%;
        width: 4px;
        background: var(--rz-primary);
        transform: scaleY(0);
        transition: transform 0.3s ease;
    }

    .mobile-nav-link:hover {
        background: rgba(102, 126, 234, 0.1);
        color: var(--rz-primary);
        transform: translateX(8px);
    }

    .mobile-nav-link:hover::before {
        transform: scaleY(1);
    }

    .nav-link {
        position: relative;
        padding-bottom: 4px;
    }

    .nav-link::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 0;
        width: 0;
        height: 2px;
        background: var(--rz-primary);
        transition: width 0.3s ease;
    }

    .nav-link:hover {
        color: var(--rz-primary) !important;
    }

    .nav-link:hover::after {
        width: 100%;
    }

    .footer-link {
        color: rgba(255,255,255,0.7);
        text-decoration: none;
        font-size: 15px;
        transition: all 0.3s;
        display: inline-block;
    }

    .footer-link:hover {
        color: white;
        transform: translateX(5px);
    }

    @@keyframes slideDown {
        from {
            opacity: 0;
            transform: translateY(-20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* Smooth Scroll */
    html {
        scroll-behavior: smooth;
    }

    /* Scroll offset for fixed header */
    #hero, #features, #how-it-works, #categories {
        scroll-margin-top: 100px;
    }

    /* Body reset */
    html, body {
        margin: 0;
        padding: 0;
        overflow-x: hidden;
        width: 100%;
        max-width: 100vw;
    }

    /* Prevent horizontal scroll */
    .rz-layout,
    .rz-header,
    .rz-body,
    .rz-footer {
        max-width: 100vw;
        overflow-x: hidden;
    }

    /* Mobile container fixes */
    @@media (max-width: 768px) {
        .rz-stack-horizontal {
            flex-wrap: wrap;
        }

        /* Ensure all containers respect viewport */
        div[style*="max-width"] {
            max-width: 100% !important;
            padding-left: 12px !important;
            padding-right: 12px !important;
        }
    }

    /* Responsive adjustments */
    @@media (max-width: 768px) {
        #hero, #features, #how-it-works, #categories {
            scroll-margin-top: 80px;
        }
    }

    .btn-compact {
        padding: 8px 20px !important;
        font-size: 14px !important;
    }

    @@media (max-width: 480px) {
        .btn-compact {
            font-size: 13px !important;
            padding: 6px 14px !important;
        }
    }

    /* Footer Styles */
    .landing-footer {
        background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
        color: white;
        padding: 60px 20px 20px;
        position: relative;
        overflow: hidden;
        margin-top: 0;
        max-width: 100vw;
        width: 100%;
    }

    .footer-decoration {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 200px;
        background: radial-gradient(circle at 20% 50%, rgba(102, 126, 234, 0.1) 0%, transparent 50%), 
                    radial-gradient(circle at 80% 80%, rgba(118, 75, 162, 0.1) 0%, transparent 50%);
        pointer-events: none;
    }

    .footer-container {
        max-width: 1400px;
        margin: 0 auto;
        position: relative;
        padding: 0 20px;
        width: 100%;
        box-sizing: border-box;
    }

    .footer-main-row {
        margin-bottom: 32px;
        margin-left: -16px;
        margin-right: -16px;
    }

    .footer-col {
        padding: 0 16px;
        margin-bottom: 32px;
    }

    .footer-brand-icon {
        width: 45px;
        height: 45px;
        border-radius: 12px;
        background: rgba(255,255,255,0.1);
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
    }

    .footer-brand-text {
        margin: 0;
        font-weight: 700;
        color: white;
    }

    .footer-description {
        color: rgba(255,255,255,0.75);
        line-height: 1.7;
        font-size: 15px;
        margin: 0;
    }

    .social-icons-container {
        flex-wrap: wrap;
    }

    .social-icon {
        width: 40px;
        height: 40px;
        border-radius: 10px;
        background: rgba(255,255,255,0.1);
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: all 0.3s;
        flex-shrink: 0;
    }

    .social-icon:hover {
        background: rgba(102, 126, 234, 0.3);
        transform: translateY(-3px);
    }

    .footer-heading {
        margin: 0;
        font-weight: 600;
        margin-bottom: 8px;
        color: white;
    }

    .footer-link {
        color: rgba(255,255,255,0.7);
        text-decoration: none;
        font-size: 15px;
        transition: all 0.3s;
        display: inline-block;
        cursor: pointer;
    }

    .footer-link:hover {
        color: white;
        transform: translateX(5px);
    }

    .contact-item {
        flex-wrap: nowrap;
        align-items: flex-start;
    }

    .contact-text {
        color: rgba(255,255,255,0.75);
        margin: 0;
        line-height: 1.5;
        word-break: break-word;
    }

    .footer-divider {
        margin: 32px 0;
        background: rgba(255,255,255,0.15);
    }

    .footer-bottom {
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-wrap: wrap;
        gap: 20px;
        padding: 20px 0;
    }

    .footer-copyright {
        color: rgba(255,255,255,0.5);
        margin: 0;
        font-size: 14px;
    }

    .footer-legal-links {
        display: flex;
        gap: 24px;
        flex-wrap: wrap;
    }

    .legal-link {
        color: rgba(255,255,255,0.5);
        text-decoration: none;
        font-size: 14px;
        transition: color 0.3s;
    }

    .legal-link:hover {
        color: rgba(255,255,255,0.9);
    }

    /* Mobile Footer Adjustments */
    @@media (max-width: 768px) {
        .landing-footer {
            padding: 40px 16px 20px;
        }

        .footer-container {
            padding: 0 8px;
        }

        .footer-main-row {
            margin-left: 0;
            margin-right: 0;
        }

        .footer-col {
            padding: 0 8px;
            margin-bottom: 40px;
        }

        .footer-brand-icon {
            width: 40px;
            height: 40px;
        }

        .footer-brand-text {
            font-size: 20px;
        }

        .footer-description {
            font-size: 14px;
        }

        .footer-heading {
            font-size: 16px;
        }

        .footer-link {
            font-size: 14px;
        }

        .contact-text {
            font-size: 14px;
        }

        .footer-bottom {
            flex-direction: column;
            align-items: flex-start;
            gap: 16px;
        }

        .footer-legal-links {
            flex-direction: column;
            gap: 12px;
            width: 100%;
        }

        .legal-link {
            font-size: 13px;
        }

        .social-icons-container {
            justify-content: flex-start;
        }
    }

    @@media (max-width: 480px) {
        .landing-footer {
            padding: 30px 12px 16px;
        }

        .footer-col {
            margin-bottom: 32px;
        }

        .footer-brand-text {
            font-size: 18px;
        }

        .footer-description {
            font-size: 13px;
            line-height: 1.6;
        }

        .footer-heading {
            font-size: 15px;
        }

        .footer-link {
            font-size: 13px;
        }

        .social-icon {
            width: 36px;
            height: 36px;
        }

        .footer-copyright {
            font-size: 12px;
        }

        .legal-link {
            font-size: 12px;
        }
    }
</style>

@code {
    private bool mobileMenuOpen = false;
    private int hoveredSocial = -1;

    private void ToggleMobileMenu()
    {
        mobileMenuOpen = !mobileMenuOpen;
    }

    private async Task ScrollToSection(string sectionId)
    {
        Console.WriteLine($"🎯 C# ScrollToSection called with: {sectionId}");
        Console.WriteLine($"📍 Current URI: {Navigation.Uri}");
        
        try
        {
            // Check if we're on home page
            var currentPath = new Uri(Navigation.Uri).AbsolutePath;
            Console.WriteLine($"📍 Current path: {currentPath}");
            
            if (currentPath != "/")
            {
                // Navigate to home first, then scroll
                Console.WriteLine($"🔄 Not on home page, navigating to /{sectionId}");
                Navigation.NavigateTo($"/{sectionId}");
            }
            else
            {
                // We're on home page, just scroll
                Console.WriteLine($"✅ On home page, scrolling...");
                
                // Close mobile menu first if open
                if (mobileMenuOpen)
                {
                    mobileMenuOpen = false;
                    StateHasChanged();
                }
                
                // Wait a bit for DOM to be ready
                await Task.Delay(100);
                
                // Try V2 first (simpler, more direct)
                Console.WriteLine($"🚀 Trying scrollToElement (V2)...");
                var success = await JSRuntime.InvokeAsync<bool>("scrollToElement", sectionId);
                
                if (!success)
                {
                    // Fallback to V1
                    Console.WriteLine($"⚠️ V2 failed, trying V1...");
                    await JSRuntime.InvokeVoidAsync("smoothScrollTo", sectionId);
                }
                
                Console.WriteLine($"✅ Scroll function called successfully");
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine($"❌ Error in ScrollToSection: {ex.Message}");
            Console.WriteLine($"Stack trace: {ex.StackTrace}");
            
            // Last resort fallback
            try
            {
                Console.WriteLine($"🔄 Last resort: trying smoothScrollToSimple...");
                await JSRuntime.InvokeVoidAsync("smoothScrollToSimple", sectionId);
            }
            catch (Exception ex2)
            {
                Console.WriteLine($"❌ All methods failed: {ex2.Message}");
            }
        }
    }

    private async Task ScrollToTop()
    {
        await ScrollToSection("#hero");
    }

    private void HoverSocial(int index)
    {
        hoveredSocial = index;
    }
}
